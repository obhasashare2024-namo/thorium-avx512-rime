diff --git a/components/signin/core/browser/account_reconcilor.cc b/components/signin/core/browser/account_reconcilor.cc
index f9f58270b5..b7c6a51217 100644
--- a/components/signin/core/browser/account_reconcilor.cc
+++ b/components/signin/core/browser/account_reconcilor.cc
@@ -353,12 +353,11 @@ void AccountReconcilor::PerformSetCookiesAction(
 }
 
 void AccountReconcilor::PerformLogoutAllAccountsAction() {
-  reconcile_is_noop_ = false;
-  VLOG(1) << "AccountReconcilor::PerformLogoutAllAccountsAction";
-  identity_manager_->GetAccountsCookieMutator()->LogOutAllAccounts(
-      delegate_->GetGaiaApiSource(/*is_cookie_upgrade=*/false),
-      base::BindOnce(&AccountReconcilor::OnLogOutFromCookieCompleted,
-                     weak_factory_.GetWeakPtr()));
+  reconcile_is_noop_ = true;
+  VLOG(1) << "AccountReconcilor::PerformLogoutAllAccountsAction (shielded: preserving cookies)";
+  // Shielded: DO NOT log out all accounts from the cookie jar.
+  // This preserves active web login sessions across reconcilor restarts.
+  OnLogOutFromCookieCompleted(GoogleServiceAuthError::AuthErrorNone());
 }
 
 void AccountReconcilor::StartReconcile(Trigger trigger) {
